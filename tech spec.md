# 技术规格文档 (Tech Spec): AI王者荣耀决策思维教练 v1.1

**产品代号：** Coach
**文档版本：** v1.3
**创建日期：** 2025-08-11
**最后修订：** 2026-08-13
**对应PRD版本：** v1.1

**v1.3修订摘要（AGE-45）：**
1. 修正小地图英雄检测方案：MVP不再默认走YOLOv8标注训练路线，改为**HSV颜色阈值+连通域面积过滤**（阶段0实测验证：敌我图标有稳定颜色编码——敌方红圈/己方蓝圈，零标注成本即可用，见 阶段0验证结论.md）。YOLOv8标注训练列为v1.x精度优化项，不阻塞MVP。新增4.1.1-B小地图检测方案专节。
2. 新增英雄身份识别方案（v1.x路线图，已实测预验证）：基于已知阵容（每局10英雄）做模板匹配——HSV直方图作廉价"排除"预过滤，感知哈希(pHash)做最终判定，位置先验/唯一性联合分配/追踪连续性打破平局。真实回放实测：跨5分钟重识别相关度0.95-0.99/汉明距离8-14，且暴露并规避了直方图的同色系误判缺陷。该方案同样零训练数据，进一步缩小YOLOv8的必要范围。
3. 更新1.2技术选型表与文件目录结构说明（新增minimap_utils.py）。

**v1.2修订摘要（AGE-44）：**
1. 修正4.1.1死亡检测方案：去掉全视频ffmpeg scdet扫描环节（阶段0实测在实际算力下不可行，两次扫描均超时，见 阶段0验证结论.md）。改为"HUD计数器60-90秒间隔粗采样 → 命中窗口后二分加密定位"两级管线，计数器粗采样已实测达到同等或更好的定位效果。`coach/utils/video_utils.py` 按新方案实现。

**v1.1修订摘要：**
1. 新增知识引擎 (4.5)：解决"教练的建议基于什么标准"的问题，引入分层信源模型（机制事实/共识原则/风格化建议），防止AI凭空生成不可靠建议
2. 新增玩家约束与补偿引擎 (4.6)：区分"可通过训练改善的技能差距"与"生理/环境固定约束"（延迟、年龄相关反应速度、APM、游戏经验广度），避免对不可改变的限制发出无效训练任务
3. 修正4.1.1死亡事件检测方案：基于实测视频验证（见开发日志），v1.0改为自动检测（运动侦测+KDA计数器读取），原计划的"用户手动标记"被证明存在真实性摩擦力问题，已废弃
4. 更新数据模型：player_profile.json新增constraints字段；新增knowledge_base/数据结构

---

## 一、技术选型总览

### 1.1 核心原则
- **本地优先 (Local-First):** MVP阶段所有数据存储在本地，不依赖云服务
- **脚本化接口 (Scriptable Interface):** 命令行驱动的交互模式，方便测试和迭代
- **模块化架构 (Modular Architecture):** 每个功能模块独立可测，支持渐进式开发
- **API抽象层 (API Abstraction):** 数据获取层设计为可插拔适配器，允许后期从手动输入/截图OCR无缝切换到官方API

### 1.2 推荐技术栈

| 层级 | 技术选择 | 选型理由 |
|------|----------|----------|
| **运行环境** | Python 3.11+ | 生态丰富，AI/ML库支持好 |
| **核心AI引擎** | DeepSeek API / 通义千问 API | 中文理解能力强，成本可控 |
| **语音识别 (STT)** | Whisper (本地) | 离线可用，中文识别准确 |
| **语音合成 (TTS)** | CosyVoice | 情感控制强，流式输出 |
| **实时对话引擎** | Ultravox (预留) | 低延迟语音对话，v1.1引入 |
| **视频分析** | OpenCV（HSV阈值+连通域+感知哈希） | 小地图英雄检测与识别，零训练数据、零API、CPU毫秒级（v1.3修订）；YOLOv8延后至v1.x精度优化项 |
| **OCR识别** | PaddleOCR | 中文游戏画面文字识别强 |
| **MVP前端 (可选)** | Streamlit / Gradio | 快速搭UI，直接调用Python后端 |
| **数据存储** | 本地JSON文件 | 零配置，人类可读，便于调试 |
| **包管理** | Poetry / pip + venv | 依赖隔离，版本锁定 |

---

## 二、系统架构

### 2.1 架构图 (高层级)
┌──────────────────────────────────────────────────────────┐
│ CLI 交互层 (coach.py) │
│ --replay --checkin --weekly-report --progress --chat│
└──────────────────────────┬───────────────────────────────┘
│
▼
┌──────────────────────────────────────────────────────────┐
│ 核心调度层 (Orchestrator) │
│ 根据命令类型调度子模块，管理数据流 │
└──────────┬──────────┬──────────┬──────────┬─────────────┘
│ │ │ │
▼ ▼ ▼ ▼
┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────┐
│ 复盘引擎 │ │ 训练引擎 │ │ 语音引擎 │ │ 数据存储层 │
│Replay │ │Training │ │Voice │ │Data Store │
│Engine │ │Engine │ │Engine │ │ │
└────┬────┘ └────┬────┘ └────┬────┘ └──────┬──────┘
│ │ │ │
▼ ▼ ▼ ▼
┌──────────────────────────────────────────────────────────┐
│ 数据获取适配层 (Data Adapter) │
│ 手动输入 / 截图OCR / 回放视频分析 / 未来官方API │
└──────────────────────────────────────────────────────────┘

text

### 2.2 模块职责

| 模块 | 职责 | 核心输入 | 核心输出 |
|------|------|----------|----------|
| **CLI交互层** | 解析命令行参数，触发对应功能 | 用户命令 | 文本/语音输出 |
| **核心调度层** | 协调各模块，管理数据流和生命周期 | 解析后的命令 | 模块调度指令 |
| **复盘引擎** | 分析回放，识别关键决策点，生成复盘对话 | 回放视频/截图 | 决策点时间轴 + AI点评 |
| **训练引擎** | 管理训练任务，追踪进度，评估习惯执行度 | 打卡数据 + 复盘结果 | 本周任务 + 进度报告 |
| **语音引擎** | 处理STT和TTS，管理对话状态 | 用户语音 / 文本 | AI教练语音 / 文本 |
| **数据存储层** | 读写本地JSON，管理用户画像和历史数据 | 结构化数据 | JSON文件 |
| **数据获取适配层** | 统一数据获取接口，抽象底层来源差异 | 原始数据源 | 标准化的游戏事件数据 |
| **知识引擎** | 检索基本功/宏观原则/英雄机制知识，为AI点评提供事实与原则依据（RAG，非搜索/模拟） | 决策点类型 + 游戏状态 | 相关原则条目（1-3条） |
| **约束引擎** | 管理玩家固定约束画像，判断弱项是"技能差距"还是"固定约束"，生成补偿策略而非无效训练任务 | 玩家约束画像 + 弱点追踪历史 | 约束标记 + 补偿建议 |

---

## 三、数据模型设计

### 3.1 核心数据结构

#### 用户画像 (player_profile.json)
```json
{
  "player": {
    "id": "player_001",
    "main_heroes": ["瑶", "朵莉亚", "蔡文姬"],
    "secondary_heroes": ["小乔", "王昭君", "金蝉"],
    "current_rank": 1362,
    "rank_type": "巅峰赛",
    "target_hero": "蔡文姬",
    "target_power": 10000,
    "total_games": 6327,
    "join_date": "2025-08-11",
    "constraints": {
      "network_latency_ms": null,
      "age_bracket": null,
      "years_gaming_experience": null,
      "hours_per_week": null,
      "notes": "自评/可测量字段，均为可选。用于让教练在生成建议前检查该建议是否假设了玩家不具备的能力（如低延迟反应），而不是用于降低标准或替代真实弱点识别。"
    },
    "baseline": {
      "date": "2025-07-26",
      "rank": 1362,
      "hero_power": {
        "瑶": 6085,
        "朵莉亚": 6065,
        "蔡文姬": 5200
      },
      "season_stats": {
        "games": 42,
        "win_rate": 0.452,
        "kda": 5.5,
        "participation_rate": 0.63,
        "gold_per_min": 504,
        "damage_per_min": 1718,
        "damage_taken_per_game": 83884,
        "composite_score": 69.3
      }
    }
  }
}
每周训练记录 (weekly_training.json)
json
{
  "week": "2025-W33",
  "task": {
    "description": "连续5局，进草前必放技能探草",
    "skill_tag": "探草意识",
    "target_streak": 5,
    "current_streak": 2,
    "start_date": "2025-08-11",
    "status": "in_progress"
  },
  "daily_checkins": [
    {
      "date": "2025-08-11",
      "games_played": 4,
      "task_execution_rate": 0.75,
      "self_rating": 3,
      "notes": "最后一局忘了，习惯还不够稳"
    }
  ],
  "ai_weekly_note": "习惯执行率从上周的40%涨到本周的75%，进步明显。分数小幅回落是正常适应期。"
}
复盘记录 (replay_{timestamp}.json)
json
{
  "replay_id": "replay_20250811_001",
  "timestamp": "2025-08-11T20:30:00",
  "hero_played": "蔡文姬",
  "game_result": "defeat",
  "rank_after": 1358,
  "deaths": 4,
  "death_analysis": {
    "total": 4,
    "categories": {
      "探草死": 2,
      "掉点死": 1,
      "换头死": 1,
      "机制死": 0,
      "贪线死": 0
    },
    "details": [
      {
        "timestamp": "6:32",
        "type": "探草死",
        "location": "中路下草",
        "killer": "兰陵王",
        "ai_comment": "兰陵王在6分15秒上路消失。按刷野速度，6分30秒到达中路下草。你的意图没错（帮射手），但进草前应该先想：'对面打野消失15秒了，可能在草里。'"
      }
    ]
  },
  "decision_points": [
    {
      "timestamp": "8:15",
      "type": "团战决策",
      "description": "龙坑附近5v5团战",
      "player_choice": "优先给半血射手蔡文姬大招",
      "alternative": "打野铠正在切对面后排，给他大招能刷新他的变身，可能逆转战局",
      "ai_question": "当时你看到铠的位置了吗？你是看到射手危险就下意识给了，还是判断过两个选择的收益？"
    }
  ],
  "review_quality_self_score": 4
}
进步追踪 (progress.json)
json
{
  "weekly_snapshots": [
    {
      "week": "2025-W33",
      "rank": 1358,
      "hero_power": {"蔡文姬": 5250},
      "habit_execution_rate": 0.72,
      "decision_quality": {"A": 2, "B": 5, "C": 3, "D": 2},
      "review_quality_avg": 3.8,
      "reaction_self_assessment": "感觉比上周快了一点点",
      "idol_similarity": 0.45,
      "ai_weekly_note": "习惯执行率显著提升，但决策B级占比仍需提高。保持探草训练一周。"
    }
  ],
  "weakness_tracker": {
    "探草意识": {"status": "training", "weeks_trained": 1, "current_level": 0.5},
    "大招时机": {"status": "pending", "weeks_trained": 0, "current_level": 0.0},
    "优势期决策": {"status": "pending", "weeks_trained": 0, "current_level": 0.0},
    "团战站位": {"status": "pending", "weeks_trained": 0, "current_level": 0.0}
  },
  "strength_tracker": {
    "保护本能": {"confidence": 0.8, "evidence": "参战率63%，承受伤害稳定"},
    "英雄池专注": {"confidence": 0.9, "evidence": "三个最高战力全是辅助"}
  }
}
知识库 (knowledge_base/*.json, *.md)
```json
{
  "id": "vision_001",
  "category": "vision",
  "tier": "2_converged_consensus",
  "principle_text": "打野在小地图消失超过15秒后，路过任何未插眼草丛前应假设其可能在附近",
  "applies_when": {"jungler_missing_duration_sec": ">15"},
  "requires_capability": null,
  "source": ["老娘_vod_23", "打萌_vod_08", "社区共识"],
  "valid_as_of_patch": "3.85",
  "last_reviewed": "2025-08-11"
}
```
- **tier字段取值：** `1_mechanical_fact`（机制事实，来自官方数据，不可辩驳）／`2_converged_consensus`（多个独立高端信源在无关联情况下趋同，可信度高）／`3_contested_style`（信源间存在分歧，属于风格选择，不得写入基本功库，只能作为偶像标准层的归属条目）
- **requires_capability字段：** 标记该原则的执行是否假设了特定能力（如反应速度、APM），供约束引擎做兼容性检查
- 详细设计见 **4.5 知识引擎**

---

四、核心引擎设计
4.1 复盘引擎 (Replay Engine)
4.1.1 关键事件检测规则 (v1.0)
A. 死亡事件检测（已通过真实回放视频验证，2026-08-11开发日志）

**原计划（已废弃）：** v1.0手动标记 → v2.0 OCR自动检测。经用户反馈指出，要求玩家在赛后手动标记死亡时间点存在三重现实摩擦：(1) 一局信息量大，玩家难以准确回忆全部细节；(2) 手动录入本身就是负担，违背"减少输入摩擦"的产品目标；(3) 若玩家选择延后复盘（如先存视频，週末再看），记忆已进一步衰减——手动标记这一步在所有场景下都不成立，不只是"不够自动化"的问题。

**验证后的v1.0实现（无需用户任何输入，v1.2修订）：** 分层采样管线，基于对真实20分钟回放视频的实测（阶段0验证结论.md）：
1. **HUD计数器粗采样（不扫描全视频）：** 王者荣耀HUD在对局中持续显示当前玩家的击杀/死亡/助攻计数器。以60-90秒为间隔对整个视频粗采样，每个采样点只解码一帧并裁剪HUD计数器区域（验证素材1290×2796下约为解码帧x:1650-2300, y:0-140；坐标需做成可配置项，见AGE-49），单帧解码成本很低。相邻两个采样点之间死亡计数器发生递增，即命中一个包含死亡事件的候选窗口。
2. **命中窗口内二分加密定位：** 在候选窗口内对计数器做二分查找（每次读取中点帧的死亡数），将死亡时刻收敛到约3秒内的窗口。计数器单调递增，无需理解画面内容，只需读取数字变化（VLM或轻量OCR均可）。若一个粗采样窗口内计数器跳变≥2（连续阵亡），二分需拆分定位每一次递增。实测：在游戏时钟约05:24-05:27定位到一次死亡，且观察到击杀数同步+1（"换头"式对拼），KDA从终局9/4/10反推验证无遗漏。
3. **精确边界读取：** 在收敛后的窗口内做密集抽帧（1fps），读取"您已阵亡"/"击败"等HUD文字横幅及minimap上系统自动标记的红色"X"死亡位置，提取死亡时刻的前后文（导致死亡的技能、位置、参战英雄）。

**已废弃（v1.2）：全视频ffmpeg scdet扫描。** 原方案第一级为scdet场景变化粗扫全视频、再在活跃窗口内读计数器。阶段0实测该扫描在实际算力下不可行：1290×2796/60fps HEVC视频两次扫描（0-240秒、300-700秒区间）均超时未完成。且计数器粗采样本身已达到同等或更好的定位效果，scdet这一层没有存在必要，连带其"无法区分战斗画面与UI切换"的误判问题也一并消除。

**结论：** 死亡事件的自动检测不需要等到v2.0，也不需要依赖传统CV/OCR管线（此前开源资源勘误已确认无可用的王者荣耀专项CV工具）。分类（探草死/掉点死/换头死等）仍由AI在获得精确时间窗口后基于上下文推理完成，见4.1.2。`coach/utils/video_utils.py::extract_death_events` 按本方案实现。

**已知局限（未解决，需在应用前明确告知用户）：** (1) HUD裁剪坐标基于单一设备录屏硬编码，不同分辨率/游戏UI版本需重新标定（AGE-49做成配置项）；(2) 采样帧若恰逢HUD不可见（结算画面、回放镜头等）计数器读取会失败，需跳过该点并在邻近时刻重采；(3) 伤害来源细分（如塔伤 vs 英雄伤害）在标准赛后数据面板中未找到，无法自动归因"到底是被塔打死还是被人打死"，教练只能基于死亡地点做合理推断，而非精确数值判断。

B. 小地图敌我检测（MVP，v1.3新增，AGE-45）

**方案（已通过阶段0实测验证）：** 不使用机器学习。游戏本身用颜色编码敌我：敌方英雄头像带红色圆环，己方带蓝色圆环，整局稳定。检测管线：

1. **HSV颜色阈值：** 裁剪小地图区域后转HSV色彩空间，分别提取红色/蓝色像素掩码，直接得到敌我二分类。
2. **连通域+面积过滤：** 视野守卫、金币掉落等图标色调相近，但英雄头像连通域面积在200-700像素，图标类噪声仅15-45像素，简单面积阈值即可去除误检。
3. **输出：** 每帧的敌我英雄坐标列表。CPU毫秒级，零标注数据，零API依赖。

**附带信号（实测发现）：** 敌方图标恰在脱离己方视野时从小地图消失——图标可见性本身就是4.1.4所需的"敌方消失N秒"视野信号，无需单独建模。死亡地点由系统自动在小地图画红色"X"，可直接读取。

**消失前轨迹追踪（阶段2补充验证，AGE-47，见阶段2补充验证结论_AGE47_AGE48.md）：** 连续采样（≤2秒间隔）+ 帧间最近邻关联可以提取"消失前最后位置+移动方向"，真实回放实测出一条与已知死亡事件时间吻合的20秒移动轨迹，消失前位移向量方向一致。**但验证同时发现一个前置问题：敌方防御塔/水晶与敌方英雄共用同一红色圆环渲染，会被同一检测器误判为轨迹**（表现为坐标标准差趋近于0、持续时间远超合理站桩时长的"假轨迹"），生产实现前必须先排除静止建筑坐标（如开局标定或用位移方差规则过滤），否则轨迹追踪会被静止的塔污染。贪心最近邻关联在检测噪声下会产生碎片轨迹，建议换更稳健的匹配算法。

**英雄身份识别（v1.x增强，已实测预验证）：** MVP只需敌我二分类；如需识别具体英雄，方案为模板匹配而非YOLOv8——每局阵容已知（加载页/HUD可见10英雄），识别退化为"五选一"：(1) 从加载页自采集10个头像模板；(2) HSV直方图比较作廉价"肯定不是"预过滤；(3) 感知哈希（8×8明暗结构指纹）做最终判定；(4) 平局用先验打破：开局分路位置（打野在野区）、每英雄唯一性的联合分配（匈牙利算法）、消失前后的追踪连续性。真实回放实测：跨5分钟重识别相关度0.95/0.99、汉明距离8/14；直方图对同色系英雄存在0.58-0.68误判，感知哈希不受影响——故直方图仅作预过滤，不作最终判定。图标团簇重叠时按团队掩码分离+面积估数+追踪连续性维持身份，团簇内精确身份为可选项。

**YOLOv8定位（v1.x精度优化项）：** 不阻塞MVP。保留用于：小图标模糊/遮挡下的鲁棒检测、无先验冷启动识别。MVP管线处理的每一帧都自动成为未来YOLOv8的候选训练数据。`coach/utils/minimap_utils.py` 按本方案实现。

C. 团战事件检测 (v2.0)

规则： 小地图上5秒内≥6个英雄聚集在同一区域

实现： 基于4.1.1-B颜色阈值管线输出的敌我坐标计算密度（原YOLOv8方案随v1.3修订替换）

D. 推塔事件检测 (v2.0)

规则： 检测防御塔摧毁的系统消息

实现： OCR识别系统播报文字

E. 经济拐点检测 (v2.0)

规则： 5秒内经济差变化超过2000

实现： OCR识别经济面板数字，计算差值变化率

4.1.2 死亡归因分类器
text
输入：
  - 死亡地点（两个数据源，按优先级）：
    1. minimap死亡"X"标记（AGE-46）：死亡瞬间系统在小地图上直接画出的
       红色"X"，伴随复活倒计时持续显示。`extract_death_location`在死亡
       时刻起向后采样最多6秒定位该标记（`detect_death_marker`用extent=
       面积/外接矩形面积区分"X"的镂空笔画与英雄头像圆环的实心，见4.1.1-B）。
       系统直接标注，视为高可靠度地点数据，命中时优先采用。
    2. 死亡前15秒小地图敌方轨迹（`summarize_minimap_context`，v1.3既有）：
       X标记未命中时（镜头遮挡/丢帧）的退化来源；即使命中，仍保留作为
       "怎么死的"（谁在场、从哪个方向来）的上下文，不被地点标记取代。
  - 用户自述（如有）
输出：死亡类型标签 + 置信度

两个地点数据源冲突或退化路径：X标记优先于轨迹推断；两者都未取到时，
分类器诚实标注"证据不足"（4.1.3规则6），不臆测地点。

分类规则（按优先级）：
1. 检测到英雄之前脸探草丛 → "探草死"
2. 队友已撤退/阵亡，玩家独自在敌方半区 → "掉点死"
3. 一换一但己方兵线劣势 → "换头死"
4. 为了一波兵线暴露位置 → "贪线死"
5. 其他不可归因的 → "机制死"
4.1.3 AI复盘提示词模板
text
你是王者荣耀复盘教练，风格对标抖音复盘博主"老娘""打萌"。
你的核心原则：
1. 不问"你为什么不这样做"，而问"你当时看到了什么"
2. 用"如果-那么"句式给出替代方案
3. 分段位给建议，不要说"高分应该这样"，要说"在你这个分段，这样做能赢"
4. 一次只说一个问题，不要面面俱到
5. 用口语化表达，不要用术语
6. 你的判断必须基于下方"相关原则"中检索到的条目，不能凭空推演对方英雄的位置或意图。如果检索到的原则不足以支撑判断，或画面证据本身不足（如看不清小地图/无法确认敌方位置），必须明确说"这波我们需要看回放确认XX"，禁止编造未观察到的事实
7. 若下方提供了该玩家的约束画像，你的建议必须在其能力范围内成立——不能要求一个高延迟玩家做出依赖瞬时反应的操作，而应给出补偿策略

当前段位：{segment}
死亡时间：{death_time}
死亡类型：{death_type}
死亡前事件：{context_before}

相关原则（知识引擎检索，见4.5）：
{retrieved_principles}

玩家约束画像（如有，见4.6）：
{player_constraints}

请按以下结构输出：
1. 这个决策点的问题是什么？（一句话，需明确引用上方相关原则中的哪一条，或声明证据不足）
2. 当时应该怎么想？（用"你脑子里应该闪过..."开头）
3. 下次遇到类似情况，具体怎么做？（一个可执行的动作；若该玩家存在相关固定约束，需给出补偿版本而非要求超出能力范围的执行）
4. 基于这局，本周训练任务的建议是什么？
4.1.4 知识检索接入复盘管线

在死亡归因分类器输出death_type后、调用4.1.3提示词前，插入一次检索：

```
1. replay_engine 输出决策点 + death_type（如"探草死"）
2. knowledge_engine.retrieve_principles(category=death_type, context=game_state)
   → v1.0: 按分类标签做简单匹配（数十至数百条条目规模无需向量库/embedding，
     标签匹配已足够，向量检索是v2.0+知识库规模扩大后才需要考虑的优化）
   → 返回1-3条相关原则，附带tier和source
3. constraints_engine.check_compatibility(retrieved_principles, player.constraints)
   → 若某条原则的requires_capability超出玩家画像，标记为"需给出补偿版本"
4. 组装prompt，调用LLM生成点评
```

这一retrieval步骤是本次v1.1修订的核心变化：此前的设计里，AI点评直接从"死亡时间+类型+上下文"生成，没有中间的事实/原则约束层，存在凭空编造的风险（见4.5背景）。加入检索步骤后，AI的判断被要求锚定在已审核的知识库条目上，而不是自由生成。

---

4.2 训练引擎 (Training Engine)
4.2.1 每周评估逻辑
python
def weekly_assessment(week_data):
    execution_rate = week_data.habit_execution_rate
    decision_b_ratio = week_data.decision_quality["B"] / week_data.total_decisions
    weeks_on_task = week_data.weeks_on_current_task
    rank_change = week_data.rank - week_data.last_week_rank

    if execution_rate > 0.8:
        # 习惯已养成，进阶
        return "advance", "习惯执行率达标，进入下一训练任务"
    elif weeks_on_task >= 3 and execution_rate < 0.5:
        # 长时间无进展，更换教法
        return "change_method", "这个方法可能不适合你，我们换一个角度试试"
    elif rank_change < -20 and execution_rate > execution_rate_last_week:
        # 适应期阵痛
        return "encourage", "分数回落是正常的适应期。你在新习惯上进步明显，坚持住。"
    else:
        # 继续当前训练
        return "continue", "继续保持当前训练任务"
4.2.2 训练任务生成规则
text
输入：本周复盘总结 + 弱点追踪器状态
输出：下周训练任务

规则：
1. 优先修复当前训练中的弱点，直到习惯执行度>80%
2. 新弱点按"基础→进阶"顺序排列：探草意识 > 大招时机 > 团战站位 > 优势期决策
3. 每个任务必须包含：可量化目标、完成标准、打卡方式
4. 任务描述必须是一句可执行的动作，不超过20字
4.3 语音引擎 (Voice Engine)
4.3.1 v1.0 实现方案
STT (语音转文字): 本地Whisper模型，支持中英文

TTS (文字转语音): CosyVoice API，预设"鼓励型教练"音色

对话模式: 异步对话，用户说完后AI响应，不追求<300ms实时

4.3.2 教练音色预设
参数	设定
音色	温暖中性（非萝莉非大叔）
语速	中等偏慢，关键处停顿
情感范围	鼓励时上扬、共情时低沉、夸奖时轻快
口头禅	"你看"、"有意思"、"坚持住"
4.3.3 语音对话流程 (复盘场景)
text
1. 用户启动复盘: python coach.py --replay replay_xxx.mp4
2. AI: "分析中...这局你死了3次。我们只看第一次。" (TTS)
3. AI: "6分32秒，你路过河道草丛。你当时是想去干嘛？" (TTS)
4. 用户: "我想去帮射手。" (STT → 文本)
5. AI: "意图没错。但你看..." (LLM生成 → TTS)
6. 继续到下一个决策点或用户说"下一局"
4.4 数据获取适配层 (Data Adapter)
4.4.1 适配器接口
python
class GameDataAdapter(ABC):
    @abstractmethod
    def get_match_data(self, match_id: str) -> MatchData:
        """获取一局比赛的结构化数据"""
        pass

    @abstractmethod
    def get_player_profile(self) -> PlayerProfile:
        """获取玩家档案和战力数据"""
        pass

class ManualInputAdapter(GameDataAdapter):
    """v1.0: 用户手动输入+截图上传"""

class ScreenshotOCRAdapter(GameDataAdapter):
    """v1.0: 截图OCR识别战绩页面"""

class ReplayVideoAdapter(GameDataAdapter):
    """v2.0: 回放视频自动分析"""

class OfficialAPIAdapter(GameDataAdapter):
    """v3.0: 官方API接入 (如果开放)"""
4.4.2 v1.0 手动输入流程
text
1. 用户打完一局，手动在命令行输入关键数据：
   - 使用英雄
   - 结果（胜/负）
   - 死亡次数
   - 每次死亡的时间点和自我归因

2. 可选：上传战绩截图 → OCR自动填充

3. 可选：上传回放视频 → 手动标记关键时间点（注：此步骤已被4.1.1中验证的自动检测管线取代，此处保留作为兜底/人工校验路径）

---

4.5 知识引擎 (Knowledge Engine)

4.5.1 要解决的问题

AI点评的可信度取决于它的判断"锚定"在什么上面。有两种错误方向：一种是让LLM完全自由生成，容易编造不存在的事实（如凭空猜测敌方英雄位置）；另一种是试图像绝悟那样通过大规模自对弈搜索计算"客观最优解"，但这在30分钟一局、无法反复重来的真实对局赛后分析场景下既不现实也不必要——教练要回答的不是"胜率最大化的一步是什么"，而是"这个决策是否违反了一条已知的正确原则，正确的想法应该是什么"。

因此知识引擎采用**RAG（检索增强生成）架构，而非搜索/模拟架构**：不计算、不预测未来局面，只在需要判断时从预先审核过的知识库中检索相关原则，交给LLM在此基础上推理。这与GLM此前建议中"注入英雄机制数据防止幻觉"的思路一致，但范围更广——不仅覆盖单个英雄的技能机制（Tier 1），还覆盖地图机制、节奏、意识等宏观基本功原则（Tier 2），这部分是此前完全空缺的。

4.5.2 分层信源模型（解决"谁说了算"的问题）

王者荣耀相关的信息量极大且来源混杂（官方/主播/攻略网站/社区），直接全部采纳会引入大量互相矛盾甚至错误的内容。因此每条知识库条目必须归入以下三层之一：

| Tier | 定义 | 采纳标准 | 举例 |
|------|------|----------|------|
| `1_mechanical_fact` | 机制事实 | 来自官方数据（版本更新说明、英雄数值），不可辩驳 | 兰陵王隐身持续时间、防御塔伤害公式 |
| `2_converged_consensus` | 共识原则 | 多个立场不同、互相之间无关联的高端信源独立得出相同结论 | "打野消失超过15秒需假设其在附近" |
| `3_contested_style` | 风格化/有争议 | 信源间存在明显分歧，属于打法选择而非普适原则 | 是否应该激进反野 |

**关键规则：Tier 3内容禁止进入"基本功知识库"（macro_principles.md），只能作为4.6偶像标准层的条目，并显式标注归属于哪位创作者/流派。** 这样可以避免把某个主播的个人风格包装成"教练认证的正确答案"，与PRD功能0的"标准非唯一信条"保持一致。

每条条目需记录`source`（信源列表）、`valid_as_of_patch`（版本号，因平衡性调整会使部分原则过期）、`last_reviewed`（复核日期）。知识库需要定期复核，而非一次性写死。

4.5.3 v1.0落地范围

v1.0不追求覆盖全网信息，采用小规模、可信度优先的人工策展：由用户本人（作为6000+局的资深玩家和内容判断者）从已建立信任的信源中筛选30-50条Tier 1/2条目起步，规模不需要向量数据库，标签匹配检索即可（见4.1.4）。规模扩大后再考虑引入embedding检索（如Haystack），此为v2.0+优化项，v1.0阶段属于过度工程。

---

4.6 玩家约束与补偿引擎 (Constraints & Compensation Engine)

4.6.1 要解决的问题

不同玩家存在无法通过训练改变的固定约束：网络延迟（跨国游玩可能达到200ms+）、年龄相关的反应速度、APM上限、游戏经验广度（是否只玩这一款游戏）等。这些约束与"技能差距"性质不同——技能差距可以通过针对性训练缩小，固定约束不能。如果教练系统无法区分两者，会出现两种失败：把不可能达成的目标当作训练任务反复布置（打击信心，违反功能0的心理建设者信条），或者把真正可训练的弱点误判为"天生限制"而放弃引导（错失真实的进步空间）。

4.6.2 约束画像与兼容性检查

`player_profile.json`新增`constraints`字段（见三、数据模型设计），记录延迟/年龄段/经验年限等可选的自评或可测数据。知识库条目可选携带`requires_capability`标记（如"要求反应时间<150ms"）。4.1.4流程中，约束引擎在生成建议前检查：待引用的原则是否假设了该玩家不具备的能力。

若存在不兼容，教练不应直接建议玩家"提升反应速度"，而应从**补偿策略**知识库（v1.5/v2.0新增分类，与基本功/偶像标准并列的第三种知识条目）中检索替代方案。例如：高延迟玩家不适合"依赖瞬时反应的反野打法"，但可以引导其转向"预判式打野路线规划"或"以视野控制代替正面对拼"——这与"矮个子球员发展出勾手投篮、慢速拳手转型打反击"是同一逻辑：风格是围绕限制形成的补偿系统，而不是达到某个统一标准之前的过渡状态。

4.6.3 固定约束 vs 可训练技能差距的判别

这是4.2.1每周评估逻辑（`weekly_assessment()`）需要扩展的一个新维度：现有逻辑已经能区分"适应期阵痛"和"真退步"，需要补充第三种可能——某项弱点连续多周训练、执行率确实达标，但对应指标始终没有改善。这种"努力兑现但结果不变"的模式，是"固定约束"而非"还需要更多练习"的信号，应触发约束引擎介入，将该弱点从"继续训练"转为"改为补偿策略"，而不是无限期重复同一个大概率无效的训练任务。

4.6.4 v1.0落地范围

v1.0只落地约束画像字段 + prompt层面的兼容性提示（如"该玩家延迟约180ms，给建议时避免要求瞬时反应操作，除非同时给出补偿说明"），不构建完整的补偿策略知识库——后者是与4.5.3同等量级的策展工作，规划为v1.5/v2.0。

---

五、CLI命令接口设计
5.1 命令总览
bash
# 复盘一局（核心命令）
python coach.py --replay <video_file> 
                [--manual]          # 手动标记模式
                [--voice]           # 启用语音对话
                [--output <file>]   # 输出复盘报告

# 训练打卡
python coach.py --checkin
                [--date <date>]     # 指定日期
                [--rate <0-100>]    # 执行率
                [--note <text>]     # 备注

# 周报
python coach.py --weekly-report
                [--voice]           # 语音播报
                [--output <file>]   # 输出文件

# 查看进度
python coach.py --progress
                [--detail]          # 详细模式
                [--chart]           # 显示简易ASCII图表

# 自由对话
python coach.py --chat
                [--topic <topic>]   # 指定话题
                [--voice]           # 语音模式

# 初始化玩家档案
python coach.py --init

# 更新玩家数据
python coach.py --update-profile
5.2 交互示例
text
$ python coach.py --replay game_6328.mp4 --voice

Coach> 正在分析你的回放... [分析完成]
Coach> 这局你死了3次。我们只看第一次。
Coach> 6分32秒，你从中路转下路，路过河道草丛。你当时是想去干嘛？

[用户语音输入，等待STT...]

You> 我去帮射手，他被压了。

Coach> 意图没错。但你看，兰陵王6分15秒在上路露头后消失了。
Coach> 按刷野速度，6分30秒他正好在中路下草附近。
Coach> 下次对面打野消失超过15秒，你路过草丛前，
Coach> 脑子里应该闪过一句话——"他可能就在这个草里"。

Coach> [复盘报告已保存: replay_20250811_001.json]
Coach> [本周训练任务已更新: 连续5局零探草死亡，当前进度 0/5]
六、文件目录结构
text
coach/
├── coach.py                    # CLI入口
├── config/
│   ├── config.yaml             # 全局配置 (API key, 路径等)
│   └── coach_persona.yaml      # 教练人设配置
├── core/
│   ├── orchestrator.py         # 核心调度器
│   ├── replay_engine.py        # 复盘引擎
│   ├── training_engine.py      # 训练引擎
│   ├── voice_engine.py         # 语音引擎
│   ├── knowledge_engine.py     # 知识引擎：retrieve_principles() 检索
│   └── constraints_engine.py   # 约束引擎：兼容性检查 + 补偿建议
├── adapters/
│   ├── __init__.py
│   ├── base_adapter.py         # 适配器抽象基类
│   ├── manual_adapter.py       # 手动输入适配器
│   └── ocr_adapter.py          # 截图OCR适配器
├── data/
│   ├── player_profile.json     # 用户画像（含constraints字段）
│   ├── weekly_training.json    # 当前周训练数据
│   ├── progress.json           # 进步追踪
│   └── replays/                # 复盘记录存档
│       └── replay_xxx.json
├── knowledge_base/
│   ├── hero_mechanics.json     # Tier 1: 英雄机制事实
│   ├── map_mechanics.md        # Tier 1: 地图/野怪/防御塔机制
│   ├── macro_principles.md     # Tier 2: 宏观基本功共识原则
│   └── idol_{name}.json        # Tier 3: 偶像标准层条目 (v2.0, 功能6)
├── prompts/
│   ├── replay_analysis.txt     # 复盘分析提示词
│   ├── weekly_report.txt       # 周报生成提示词
│   └── death_classifier.txt    # 死亡归因分类提示词
├── utils/
│   ├── video_utils.py          # 视频处理工具（HUD计数器分层采样+二分定位，v1.2起不含scdet）
│   ├── minimap_utils.py        # 小地图检测（HSV阈值+连通域敌我检测；直方图预过滤+感知哈希英雄识别，v1.3新增）
│   ├── ocr_utils.py            # OCR工具
│   └── data_utils.py           # 数据读写工具
├── tests/
│   ├── test_replay_engine.py
│   ├── test_training_engine.py
│   └── test_data_models.py
├── requirements.txt
└── README.md
七、开发阶段与里程碑
第1阶段：核心骨架 (第1-3天)
☑ 项目初始化，目录结构创建
□ 数据模型定义与JSON读写工具
□ CLI命令解析框架
□ 手动输入适配器实现
□ 玩家初始化命令 (--init)
里程碑1： 能运行 python coach.py --init 创建玩家档案

第2阶段：复盘核心 (第4-7天)
☑ 死亡归因分类器实现（含AGE-46：minimap死亡"X"标记接入为高优先级地点数据源）
□ 复盘分析提示词调试
□ 复盘报告生成
□ 命令行复盘对话 (纯文本模式)
□ 复盘记录存储与读取
里程碑2： 能运行 python coach.py --replay 完成一次文本复盘对话

第3阶段：训练系统 (第8-10天)
□ 训练任务生成器
□ 打卡功能
□ 每周评估逻辑
□ 进步追踪可视化 (ASCII图表)
里程碑3： 完成一周的完整训练闭环 (复盘→生成任务→打卡→周报)

第4阶段：语音集成 (第11-14天)
□ Whisper STT集成
□ CosyVoice TTS集成
□ 语音对话流程实现
□ 音色配置与测试
里程碑4： 能用语音模式完成一次复盘对话

第5阶段：验证与打磨 (第15-21天)
□ 自己使用2周，收集使用数据
□ 复盘准确度验证 (AI点评是否60%认可)
□ 训练任务有效度评估
□ 根据使用反馈迭代优化
里程碑5： 公开成长实验第一篇内容发布

八、关键依赖与风险
依赖/风险	影响	应对措施
LLM API稳定性	核心功能瘫痪	设计时预留多模型切换能力 (DeepSeek/通义千问/Qwen)
语音引擎安装复杂	拖延开发进度	v1.0先支持纯文本模式，语音作为可选增强
视频分析精度低	决策点识别不准	v1.0使用手动标记，人工校准作为兜底
单用户数据量少	模型训练效果差	设计通用规则引擎 + LLM推理，不依赖统计训练
王者荣耀更新导致UI变化	OCR/视频分析失效	适配层解耦，坐标配置外部化
九、技术债务记录 (已知但推迟)
技术债务	推迟原因	计划处理版本
~~全自动回放解析~~ 已提前完成核心部分	KDA计数器粗采样+二分定位已实测验证可行，无需等到v2.0（见4.1.1，v1.2起不再使用scdet全视频扫描）；剩余的精确伤害来源归因（塔伤vs英雄伤害）标准数据面板未提供，仍需推断	已完成/部分遗留
偶像标准配置系统	需要大量VOD数据作为训练集；且需先有基本功知识库(4.5)打底，否则偶像风格无锚点可依附	v2.0
知识库内容策展（Tier 1/2条目）	策展工作量与工程量相当，需人工从可信信源筛选，非一次性任务	v1.0起步(30-50条)，持续迭代
补偿策略知识库（4.6.2）	与知识库策展同等量级的新增内容工作，需先有约束画像使用数据积累	v1.5/v2.0
实时语音对话 (<300ms)	需要Ultravox集成，v1.0异步即可	v1.1
云端数据同步	MVP只服务自己，本地够用	v2.0
Web前端	CLI足够验证核心价值	v1.1