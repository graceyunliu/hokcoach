# 阶段0技术验证结论：Minimap检测 + 死亡检测融合

**验证日期：** 2026-08-11
**验证素材：** `Replay/ScreenRecording_08-11-2026 15-57-29_1.MP4`（1290×2796竖屏，60fps，HEVC，时长约1172秒/19.5分钟，实际游戏时长约14-15分钟）
**对应实现计划：** 阶段0（技术验证），里程碑0

---

## 一、结论摘要

**里程碑0已达成：** 在不依赖任何用户手动标记的前提下，成功从回放视频中定位出一次完整死亡事件（时刻+死亡前敌方位置上下文），且minimap检测和死亡检测两条管线都得到独立验证，可以进入阶段1核心骨架开发。

三个关键发现，其中两个是好消息（比预期更容易），一个是需要注意的工程限制：

1. **Minimap英雄识别比预期简单：** 不需要一开始就上YOLOv8训练。敌我双方用不同颜色的圆环包裹英雄头像（敌方=红色圆环，己方=蓝色圆环），用简单的HSV颜色阈值+连通域检测就能可靠区分敌我，且头像本身清晰可辨识具体英雄。
2. **死亡地点系统会自动标记：** 死亡后，游戏UI会在minimap上直接画一个红色"X"标出死亡位置，还会显示死亡英雄的头像+复活倒计时。这比"从头像消失位置反推死亡地点"要可靠得多。
3. **工程限制：** 用ffmpeg scdet对完整19.5分钟60fps视频做全量场景扫描，在当前沙箱算力下会超时（两次尝试均未在3分钟内完成）。改用"HUD KDA计数器粗采样+二分定位"的方式，不扫描全视频，只在候选窗口内加密采样，效率更高，且已验证可行。

---

## 二、Minimap检测验证细节

**方法：** 从`frame_300.jpg`（游戏内约1:58时刻）裁剪左上角420×320像素区域，用OpenCV做HSV颜色阈值检测。

**发现：**
- 己方英雄：蓝色圆环包裹英雄头像（清晰可辨认具体英雄，非抽象图标）
- 敌方英雄：红色圆环包裹英雄头像
- 干扰项：视野守卫图标、金币掉落图标（橙色菱形）也带有近似色调，需要用连通域面积过滤（英雄头像圆环的像素面积明显大于图标类元素，测试中英雄头像连通域面积普遍在200-700像素，图标类多在15-45像素）

**跨时间点验证：** 在10分钟（frame_600）和15分钟（frame_900）两个时间点检查同一区域，观察到敌方图标会随着"是否在己方视野内"而消失/出现——这与tech spec 4.1.4设计的"敌方消失超过N秒"逻辑的数据前提完全吻合，即minimap上敌方图标的可见性本身就是一个可靠的"视野"信号，不需要额外建模。

**结论：** MVP阶段可以先用颜色阈值+连通域检测方案，不需要一开始就投入YOLOv8标注训练，大幅降低阶段0到阶段2之间的工程成本。YOLOv8可以作为v1.x的精度优化项保留。

---

## 三、死亡检测验证细节

**方法调整：** 原计划先用ffmpeg scdet对全视频做场景变化扫描，找到候选窗口后再读HUD。实测中，全视频scdet扫描两次均超时未完成（分别处理0-240秒和300-700秒区间），推测是1290×2796分辨率+60fps的HEVC解码开销较大。

改为更轻量的方法：直接以60-90秒为间隔，对整个视频做粗采样，只裁剪HUD区域（击杀/死亡/助攻计数器，位置约在原始帧x:1650-2300, y:0-140），单帧解码成本很低。通过读取粗采样序列中死亡计数器的变化，定位到发生死亡的大致60-90秒窗口，再在该窗口内二分加密采样，精确定位到3秒内的窗口。

**实测结果：** 在游戏时钟约05:24-05:27（对应录制视频约第506-509秒）定位到一次死亡事件，同时观察到击杀数也同步+1——说明这是一次"换头"式对拼（双方互换一命），而非单纯被击杀。该窗口内的minimap同时显示出红色"X"死亡标记和队伍复活倒计时UI，为后续死亡归因分类提供了直接可用的证据。

**结论：** "计数器粗采样+二分定位"的死亡检测方法可行且成本可控，不依赖对全视频做重量级场景扫描。生产实现时，`utils/video_utils.py`应该采用这种分层采样策略，而不是tech spec原描述的"全视频scdet扫描"方案——这是本次验证对4.1.1技术方案的一处修正。

---

## 四、对tech spec / 实现计划的修正建议

| 原方案 | 修正后 | 原因 |
|--------|--------|------|
| 4.1.1死亡检测：全视频ffmpeg scdet粗扫 + HUD计数器二分定位 | 改为：HUD计数器60-90秒间隔粗采样 + 命中窗口后二分加密定位，跳过全视频scdet环节 | 全视频scdet扫描在当前算力下不可行（两次超时），且计数器粗采样本身已经能达到同等或更好的定位效果，无需scdet这一层 |
| minimap检测：默认走YOLOv8训练路线 | MVP先用HSV颜色阈值+连通域面积过滤方案 | 实测发现敌我图标有稳定的颜色编码（红/蓝圆环），颜色方案零标注成本即可用，YOLOv8留作精度优化项 |
| 死亡地点：需要从死亡前敌方轨迹反推位置 | 死亡后minimap会直接显示红色"X"标记，可直接读取，不需要纯靠推理 | 实测中直接观察到该UI元素，是死亡归因分类器4.1.2的额外可靠数据源 |

---

## 五、遗留的开放问题（未在本次验证中覆盖）

- 敌方英雄消失后的minimap坐标追踪（判断"消失前最后位置+移动方向"）尚未验证，本次只验证了"检测到/检测不到"的二元信号，还未做连续轨迹追踪
- 队友（非玩家本人）身份区分（区分5个蓝环头像具体是哪个队友）尚未验证，本次只验证了敌我二分类
- HUD区域坐标（如本文档中的裁剪坐标）是基于这一份录屏的分辨率/UI布局硬编码的，不同设备分辨率或游戏UI版本更新后需要重新标定，量产实现时需要做成可配置项而非写死坐标

---

## 六、下一步建议

阶段0的核心问题（"死亡事件+minimap坐标能否全自动提取"）已经验证通过，建议进入**阶段1核心骨架**开发。上述"遗留开放问题"不阻塞阶段1启动，可以在阶段2复盘引擎开发过程中按需补充验证。
