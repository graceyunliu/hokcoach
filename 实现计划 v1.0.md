# 实现计划 (Implementation Plan): AI王者荣耀决策思维教练

**产品代号：** Coach
**文档版本：** v1.0
**创建日期：** 2026-08-11
**对应PRD版本：** v1.1
**对应Tech Spec版本：** v1.1
**性质：** 本文档是对tech spec第七章"开发阶段与里程碑"的落地修订，基于与用户的实现计划brainstorm讨论产出，不改变tech spec的架构设计，只调整MVP范围和执行顺序。

---

## 一、相对tech spec的三个关键调整

1. **语音层整体移出MVP。** Voice Engine（Whisper/CosyVoice/Ultravox）不在v1.0依赖清单中。核心价值（决策链复盘、反事实推演、归因裁判）用纯文字交互即可完整验证，语音是锦上添花，不是MVP必要条件。
2. **新增"minimap英雄坐标检测"为独立验证项，且优先级不低于死亡检测。** 原tech spec 4.1.1只验证了"死亡时刻+HUD数字"的检测管线，但死亡归因分类器（4.1.2的"探草死"等标签）成立的前提是教练知道死亡时敌方英雄大致在哪。这一环此前完全没有验证过，是目前风险最高的技术未知项。
3. **技术验证阶段前移，排在核心骨架之前。** 原计划顺序是骨架→复盘核心→训练系统→语音→验证。修订后：技术验证（minimap+死亡检测融合）排第一，因为这是复盘引擎能否成立的地基，越早验证，返工成本越低。

---

## 二、修订后的阶段划分

### 阶段0：技术验证（新增，最优先）

**目标：** 证明"从一段回放视频里，自动提取出死亡时刻 + 死亡时敌方英雄大致位置"这件事可行，不需要用户手动标记。

**任务：**
- [ ] 用 `Replay/ScreenRecording_08-11-2026 15-57-29_1.MP4` 作为测试素材，截取小地图区域
- [ ] minimap英雄坐标检测：YOLOv8 + 少量标注，验证能否区分敌方/我方英雄图标并给出大致区域坐标（不追求像素级精度，"河道草丛附近" vs "在下路" 这种粗粒度区分即可）
- [ ] 复用已验证的死亡检测管线（ffmpeg scdet运动侦测 + HUD KDA计数器读取），确认在新素材上依然有效
- [ ] 把两者拼接：输出格式为 `{死亡时刻, 死亡地点, 死亡前15秒敌方英雄大致位置轨迹}`
- [ ] 已知局限记录：minimap检测无法处理隐身/草丛内英雄（本来就看不见），这属于预期内的"证据不足"场景，复盘引擎的prompt需要能诚实说"看不清，需要你确认"（tech spec 4.1.3规则6已覆盖）

**产出：** `utils/video_utils.py` 核心函数：死亡事件提取 + minimap坐标提取，均基于同一份回放视频输入。

**里程碑0：** 能对一局完整回放视频，自动输出结构化的"死亡时刻+地点+敌方轨迹"列表，无需任何手动标记。

---

### 阶段1：核心骨架（沿用tech spec Phase 1，去掉语音相关初始化）

- [ ] 项目初始化，目录结构创建（见下文"目录结构修订"）
- [ ] 数据模型定义与JSON读写工具（player_profile / weekly_training / progress / replay）
- [ ] CLI命令解析框架（`--init` `--replay` `--checkin` `--weekly-report` `--progress` `--chat`，均为纯文本交互，暂不加 `--voice` 参数）
- [ ] 手动输入适配器实现（ManualInputAdapter，作为阶段0视频管线的兜底/校验路径）
- [ ] 玩家初始化命令 `--init`

**里程碑1：** 能运行 `python coach.py --init` 创建玩家档案。

---

### 阶段2：复盘核心（沿用tech spec Phase 2，接入阶段0产出）

- [ ] 死亡归因分类器（4.1.2）：接入阶段0输出的"死亡时刻+地点+敌方轨迹"，套用分类规则（探草死/掉点死/换头死/贪线死/机制死）
- [ ] 知识引擎最小版本：先用手动整理的10-20条起步条目跑通检索逻辑（`knowledge_engine.retrieve_principles()`），不等知识库冷启动全部完成再开发
- [ ] 复盘分析提示词调试（4.1.3模板，含"相关原则"和"约束画像"注入）
- [ ] 复盘报告生成 + 命令行文本对话（`--replay`，无 `--voice` 参数）
- [ ] 复盘记录存储与读取

**里程碑2：** 能运行 `python coach.py --replay <video>` 完成一次全自动（无手动标记）的文本复盘对话，且点评引用了知识库中的具体原则条目。

---

### 阶段3：训练系统（沿用tech spec Phase 3，不变）

- [ ] 训练任务生成器
- [ ] 打卡功能
- [ ] 每周评估逻辑（含4.2.1的适应期识别 + 4.6.3的固定约束识别）
- [ ] 进步追踪可视化（ASCII图表）

**里程碑3：** 完成一周的完整训练闭环（复盘→生成任务→打卡→周报）。

---

### 阶段4：验证与打磨（沿用tech spec Phase 5，去掉语音验证项）

- [ ] 自己使用2周，收集使用数据
- [ ] 复盘准确度验证（AI点评是否≥60%认可，对应PRD成功指标）
- [ ] 训练任务有效度评估
- [ ] 根据使用反馈迭代优化

**里程碑4：** 公开成长实验第一篇内容发布。

---

### 语音层（移出MVP，标记为v1.1+可选增强）

Whisper STT + CosyVoice TTS + 语音对话流程，作为文本版验证成功后的可选增强，不设固定排期。

---

## 三、知识库冷启动计划（与阶段0-2并行推进）

**信源策略：** 用户常看的复盘博主主要在抖音和YouTube两个平台，两者转录成本差异大，建议分批推进：

1. **YouTube优先跑通管线。** 官方transcript API可直接拿字幕，零ASR成本。先用YouTube上能找到的对应内容验证"转录→提炼→打tier标签"的完整流程。
2. **抖音其次。** 无官方字幕接口，需要下载视频后本地跑Whisper做ASR，再进入同一套提炼流程。技术上和阶段0的minimap检测不冲突（都是视频处理，可复用部分工具链），但工作量更大，排在YouTube管线跑顺之后。
3. **官方数据源（Tier 1机制事实）：** 参考 `qing762/honor-of-kings-api` 的思路，从pvp.qq.com类官方页面抓取英雄数值/技能机制，作为Tier 1条目的起点，这部分不涉及博主转录，可以最先做。

**提炼流程：**
```
视频 → 转录文本（YouTube API / Whisper） → LLM辅助初筛候选原则条目
  → 用户人工判定：
     - 是否属于Tier 1（机制事实，可直接查证）/ Tier 2（需要判断是否多信源独立趋同）/ Tier 3（个人风格，只能进偶像标准层不进基本功库）
     - 标注 source / valid_as_of_patch / last_reviewed
  → 写入 knowledge_base/*.json 或 macro_principles.md
```

**目标：** 30-50条Tier 1/2条目起步（tech spec 4.5.3既定目标），阶段2里程碑2达成前至少备好10-20条以支撑复盘对话的最小可用性。

**责任划分：** 转录管线和初筛工具由开发侧（我）搭建，最终的tier判定和"是否可信、是否多源独立趋同"的判断只能由用户本人把关——这是知识库可信度的核心防线，不能自动化掉。

---

## 四、目录结构修订

在tech spec六、文件目录结构基础上，MVP阶段去掉语音相关文件，新增minimap检测工具：

```
coach/
├── coach.py
├── config/
│   ├── config.yaml
│   └── coach_persona.yaml
├── core/
│   ├── orchestrator.py
│   ├── replay_engine.py
│   ├── training_engine.py
│   ├── knowledge_engine.py
│   └── constraints_engine.py
│   （voice_engine.py 移出MVP，v1.1+再加回）
├── adapters/
│   ├── base_adapter.py
│   ├── manual_adapter.py
│   └── ocr_adapter.py
├── data/
│   ├── player_profile.json
│   ├── weekly_training.json
│   ├── progress.json
│   └── replays/
├── knowledge_base/
│   ├── hero_mechanics.json
│   ├── map_mechanics.md
│   └── macro_principles.md
├── prompts/
│   ├── replay_analysis.txt
│   ├── weekly_report.txt
│   └── death_classifier.txt
├── utils/
│   ├── video_utils.py       # 阶段0核心产出：死亡检测 + minimap坐标检测
│   ├── ocr_utils.py
│   ├── transcript_utils.py  # 新增：YouTube/抖音转录管线，服务于知识库冷启动
│   └── data_utils.py
├── tests/
├── requirements.txt
└── README.md
```

---

## 五、开放问题（需要在对应阶段开始前确认）

| 问题 | 影响阶段 | 建议决定时机 |
|------|----------|--------------|
| minimap坐标检测的具体标注方式（人工标几帧起步，还是找现成小样本数据集） | 阶段0 | 阶段0启动时 |
| YouTube上能找到多少目标博主的对应内容（老娘/打萌是否有搬运或官方账号） | 知识库冷启动 | 阶段0并行期 |
| 知识库最终存储是JSON文件还是需要更结构化的数据库 | 阶段2 | 里程碑2前，视条目量决定 |
| 阶段0若minimap检测精度不够（比如只能判断"消失"而非"大致坐标"） | 阶段2 | 阶段0结束时复盘，必要时降级为"消失时长"这一单一信号 |

---

## 六、修订记录

| 版本 | 日期 | 修订内容 |
|------|------|----------|
| v1.0 | 2026-08-11 | 首版实现计划，基于与用户的brainstorm讨论：语音移出MVP、新增minimap检测验证项并前置为阶段0、明确知识库冷启动的信源策略（YouTube优先/抖音次之） |
