name: Coach tests

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: coach/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r coach/requirements.txt pytest

      - name: Run unit and API tests
        run: python -m pytest coach/tests -q

      - name: Run knowledge-base audit
        working-directory: coach
        run: python -m tools.audit_knowledge_base --json
