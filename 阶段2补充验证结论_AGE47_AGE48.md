# 阶段2补充验证结论：AGE-47（敌方消失后轨迹追踪）+ AGE-48（队友身份区分）

**验证日期：** 2026-08-13
**验证素材：** `Replay/ScreenRecording_08-11-2026 15-57-29_1.MP4`（同阶段0验证素材）+ 同批录屏截取的真实minimap图标
**对应工单：** AGE-47、AGE-48（阶段0验证结论"五、遗留的开放问题"中列出的两项）

---

## 一、结论摘要

两项开放问题都已验证：**信号本身存在且可提取，但都需要在现有阶段0管线基础上补一层过滤/匹配逻辑才能可靠使用**，不是"直接能用"也不是"做不到"。

- **AGE-47（轨迹追踪）：** 消失前"最后位置+移动方向"确实可以从连续采样中算出来，但验证过程中发现一个阶段0未捕捉到的问题——敌方防御塔/水晶和敌方英雄用同一套红色圆环渲染，会被同一检测器误判为"英雄轨迹"（且因为塔不动，反而会产出最长最"稳定"的假轨迹）。生产化前必须先排除静止建筑，否则轨迹追踪功能会被静止的塔干扰。
- **AGE-48（队友身份区分）：** 已通过既有验证证实可行——用感知哈希（pHash）比对头像指纹，结合"阵容已知、5选1"的约束，能可靠区分5个蓝环具体是哪位队友；HSV直方图方法在同色系英雄间会误判，不适合做最终判据，只能当粗筛。

---

## 二、AGE-47验证细节：轨迹追踪

**方法：** 在阶段0已定位的死亡事件（约506-509秒）前后取440-530秒共90秒窗口，以2秒间隔连续采样minimap并跑`detect_hero_icons`，用贪心最近邻把逐帧检测到的红色图标关联成轨迹。

**发现：**

1. **移动轨迹可提取，方向可算。** 一条轨迹从(237,39)（中上区域/敌方上路附近）在20秒内连续移动到(239,122)（地图中部/河道），趋势方向一致（持续往中路/河道方向移动），消失前最后4秒的位移向量为(-12.7,-12.2)，速度约4.4px/s——这与该窗口内已知的换头死亡事件（阶段0验证结论三）时间高度吻合，说明这条轨迹很可能就是参与那次换头的敌方英雄。**该英雄在514-516秒之间从图标上消失**，消失前的位置+方向数据完整可用。

2. **关键新发现——静止建筑会污染轨迹追踪。** 同一窗口内还关联出两条"轨迹"：一条90秒内坐标标准差仅0.1-0.2像素（几乎零位移），另一条50秒内标准差5px/2.4px（近似零位移）。两者持续时间远超任何英雄会静止不动的合理时长，且位置在整个90秒窗口内跨越了阶段0已知的死亡事件前后都不消失——判断为敌方防御塔/水晶（同样用红色渲染，阶段0的3个时间点抽样验证从未连续追踪过，没有暴露这个问题）。
3. **贪心逐帧关联本身有噪声。** 部分英雄图标在单帧内会被连通域检测拆成2个相邻分量（如442秒同时出现(323,83)和(331,83)两个几乎重合的红色分量），导致最近邻关联产生大量长度3-8点的碎片"幽灵轨迹"。生产实现需要做去重（同帧内距离过近的分量合并）+ 更稳健的匹配（如匈牙利算法）而非纯贪心。

**结论：** AGE-47的核心问题——"消失前最后位置+移动方向能否提取"——回答是**可以**，但阶段0的颜色阈值方案目前无法区分"移动的英雄"和"静止的建筑"，这是投入阶段2复盘引擎前必须补的一层过滤（例如：开局前几秒标定己方视野内敌方基地/防御塔的固定坐标并排除，或用"位移方差低于阈值超过X秒"规则过滤静止目标）。

---

## 三、AGE-48验证细节：队友身份区分

**方法：** 复用阶段0已验证的红/蓝圆环检测，从同一段回放的真实图标中，取约5:00时刻的3个图标（敌方法师、敌方战士、玩家自己）作为模板，用约10:00时刻的对应图标做查询，分别用HSV直方图相关性和感知哈希（pHash）汉明距离两种方法做匹配，见`hero_id_histogram_vs_phash_demo.png`。

**发现：**

1. **pHash可靠，HSV直方图不可靠。** 两种方法都能正确重新识别出5分钟后回归的同一英雄（法师：直方图相关性0.95/哈希距离8；战士：0.99/14）。但直方图在真实数据上暴露了颜色混淆问题——对两个配色相近但实际不同的英雄给出0.68的虚高相似度，还给一个模板外的己方队友（战士行）打出0.58的误导性"匹配"；pHash两次都没有被骗，因为它比较的是明暗结构指纹而非颜色配比。
2. **"5选1"约束大幅降低了识别难度。** 对局阵容开局即已知（10个英雄，加载页+HUD可见），队友识别不是开放集识别，而是从已知的5个候选英雄中选一个——用小型自采集头像模板库做pHash匹配即可，不需要训练模型。
3. **仍需要平局打破规则。** 当pHash对多个候选给出接近的距离时（理论上限：5个队友里最多有同英雄的可能性极低但外观相近的英雄存在），需要辅助规则：开局1分钟内的分路位置（打野在野区出现）、每个英雄在一局内只会被分配一次（唯一匹配）、以及AGE-47验证的轨迹连续性（同一图标帧间位移不能突变）。

**结论：** AGE-48的核心问题——"5个蓝环头像对应哪位队友"——**已验证可行**，方案是pHash指纹匹配（非训练模型）+ 已知阵容的唯一分配约束，HSV颜色只能当作代价低的"肯定不是"粗筛，不能做最终判据。

---

## 四、对tech spec的修正建议

| 原状态 | 修正后 | 原因 |
|--------|--------|------|
| 4.1.4敌方轨迹：仅有"检测到/检测不到"二元信号 | 补充：消失前最后位置+移动方向可从连续采样+最近邻轨迹关联中提取，但必须先排除静止建筑（防御塔/水晶同为红色圆环） | AGE-47验证发现的新增前置步骤，未过滤会产出假轨迹 |
| 队友身份：未定义方案 | 采用pHash头像指纹匹配（非YOLOv8）+ 已知阵容5选1唯一分配约束；HSV直方图仅做粗筛 | AGE-48验证证实HSV直方图在真实数据上会颜色混淆误判，pHash两次验证均未误判 |

---

## 五、仍然开放的问题

- 静止建筑的坐标排除方案尚未实现（需要标定或用位移方差规则），本次只是发现问题并给出候选方案，未选型验证
- 轨迹关联算法目前是最简贪心最近邻，产生较多碎片轨迹，生产实现建议换更稳健的匹配（如匈牙利算法）+ 帧内去重
- AGE-48的"平局打破规则"（分路位置/轨迹连续性）尚未做真实数据验证，只是基于已有信号设计的方案，建议阶段2实现时用更多真实对局验证
